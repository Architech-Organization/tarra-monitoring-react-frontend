# Tarra Monitoring React Frontend Environment Variables

# Azure AD Configuration
VITE_AZURE_CLIENT_ID=your-azure-app-client-id
VITE_AZURE_TENANT_ID=your-azure-tenant-id
VITE_AZURE_REDIRECT_URI=http://localhost:5173

# Backend API Configuration
VITE_API_BASE_URL=http://localhost:8000
VITE_API_TIMEOUT=30000

# Application Configuration
VITE_APP_NAME=Tarra Monitoring System
VITE_APP_VERSION=1.0.0
VITE_APP_ENVIRONMENT=development

# Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_OFFLINE_MODE=false

# Development Settings
VITE_ENABLE_DEBUG=true
VITE_ENABLE_MSAL_LOGGING=true
VITE_LOG_LEVEL=info

# Security Settings
VITE_ENABLE_CSP=true
VITE_ENABLE_HTTPS_REDIRECT=false
VITE_SESSION_TIMEOUT=3600000

# Performance Settings
VITE_ENABLE_SERVICE_WORKER=true
VITE_ENABLE_CODE_SPLITTING=true

# Monitoring & Analytics
VITE_APPLICATION_INSIGHTS_CONNECTION_STRING=your-app-insights-connection-string
VITE_ENABLE_TELEMETRY=false

# Optional: Direct login for development
# VITE_SKIP_AUTHENTICATION=false

# Optional: Mock data for development
# VITE_USE_MOCK_DATA=false

# IMPORTANT SECURITY NOTES:
# - Never commit actual secrets to git
# - Use different values for different environments
# - In production, use environment-specific configuration
# - Consider using Azure Key Vault for sensitive values

# Common Issues & Troubleshooting:
# 1. If you see authentication loops, verify:
#    - VITE_AZURE_CLIENT_ID is correct
#    - VITE_AZURE_TENANT_ID is correct  
#    - VITE_AZURE_REDIRECT_URI matches your app registration
#
# 2. If MSAL fails to initialize:
#    - Check browser console for specific error messages
#    - Verify your Azure app registration settings
#    - Ensure redirect URIs are correctly configured
#
# 3. For debugging authentication issues:
#    - Set VITE_ENABLE_DEBUG=true
#    - Set VITE_ENABLE_MSAL_LOGGING=true
#    - Check the AuthDebugger component in development

# Production Deployment Notes:
# - Update VITE_AZURE_REDIRECT_URI to your production domain
# - Set VITE_API_BASE_URL to your production API endpoint
# - Disable debug settings in production
# - Enable security headers and HTTPS redirect